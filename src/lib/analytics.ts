// Vercel Analytics removed — trackGameEvent is now a no-op.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function trackGameEvent(_eventName: string, _payload?: Record<string, unknown>) {
  // no-op
}
